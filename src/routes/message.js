const handleConnect = (socket) => {
  // Logic for handling on connect event
}

const handleDisconnect = (socket) => {
  // Logic for handling on disconnect event
}

const handleDeleteNotifai = (socket, data) => {
  // Logic for handling on delete notifai event with data in the body
}

const handleUpdateNotifai = (socket, data) => {
  // Logic for handling on update notifai event with data in the body
}

module.exports = {
  handleConnect,
  handleDisconnect,
  handleDeleteNotifai,
  handleUpdateNotifai
}
